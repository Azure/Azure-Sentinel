ParserQuery: |
    let ProcessEvents_M365D=(){
    DeviceProcessEvents
    |
    project
    //***** Creating new fields & projecting existing fields ******
        TimeGenerated,
        EventCount=int(1),
        EventProduct="M365 Defender for Endpoint",
        EventVendor="Microsoft",
        EventSchemaVersion="0.0.1",
    //***** Renaming Fields ****** 
        EventOriginalUid = ReportId,
        EventType = ActionType
    //TODO: review all missing fields and add to query
    };
    ProcessEvents_M365D