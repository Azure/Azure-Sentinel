id: 'ask tj ???'
name: Service Principal Not Using Client Credentials
description: |
  'An service principal is not using a client certificate or secret is not secure. 
   It is recommended that you review your needs and use an Authentication method 
   for sign-in.",20012,"'servicePrincipal':'OfficeClientService'
requiredDataConnectors:
  - connectorId: '???'
    dataTypes:
      - Senserva Control
tactics:
  - Impact
query: |
  let timeframe = 60d;
  DashboardTest_CL
  | where TimeGenerated >= ago(timeframe)
  | where ControlName_s == 'ServicePrincipalNotUsingClientCredentials'