{
  "tool_name": "{{TOOL_NAME}}",
  "display_name": "{{TOOL_DISPLAY_NAME}}",
  "description": "{{TOOL_DESCRIPTION}}",
  "parameters": {
    "type": "object",
    "properties": {
      "{{PARAM_1_NAME}}": {
        "type": "string",
        "description": "{{PARAM_1_DESCRIPTION}}"
      }
    },
    "required": ["{{PARAM_1_NAME}}"]
  },
  "kql_query": "{{TABLE_NAME}}_CL\n| where TimeGenerated > ago(24h)\n| where {{FILTER_COLUMN}} has '{{{{PARAM_1_NAME}}}}'\n| summarize count() by {{GROUP_BY}}",
  "workspace": "{{WORKSPACE_ID}}",
  "notes": [
    "Custom MCP tool for {{ISV_NAME}}",
    "Executes against Sentinel Data Lake",
    "Reference: https://learn.microsoft.com/en-us/azure/sentinel/datalake/sentinel-mcp-create-custom-tool"
  ]
}
