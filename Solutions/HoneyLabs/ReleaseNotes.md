| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History**                    |
|-------------|--------------------------------|---------------------------------------|
| 3.0.1       | 12-08-2026                     | Workbook now charts indicator activity by the date the activity happened rather than the date the connector polled, so a new connection shows real history instead of a single point. Added a match panel for normalised network data (ASIM) alongside the existing CEF panel, and both explain themselves when the underlying table is absent. Matched addresses and probed CVEs link through to the evidence behind them. |
| 3.0.0       | 25-07-2026                     | Initial solution release: TAXII data connector for the HoneyLabs attacker-IP collection (CVE probers included on paid plans), four analytic rules on the ThreatIntelIndicators table, four hunting queries, threat intelligence workbook, IP enrichment playbook. |
