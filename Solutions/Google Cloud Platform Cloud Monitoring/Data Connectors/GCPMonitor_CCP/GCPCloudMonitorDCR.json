{
    "name": "GCPMonitorDCR",
    "apiVersion": "2022-06-01-preview",
    "type": "Microsoft.Insights/dataCollectionRules",
    "location": "{{location}}",
    "properties": {
        "dataCollectionEndpointId": "{{dataCollectionEndpointId}}",
        "streamDeclarations": {
            "Custom-GCPMonitor": {
                "columns": [
                    {
                        "name": "resource",
                        "type": "dynamic"
                    },
                    {
                        "name": "metric",
                        "type": "dynamic"
                    },
                    {
                        "name": "metricKind",
                        "type": "string"
                    },
                    {
                        "name": "valueType",
                        "type": "string"
                    },
                    {
                        "name": "points",
                        "type": "dynamic"
                    }
                ]
            }
        },
        "destinations": {
            "logAnalytics": [
                {
                    "workspaceResourceId": "{{workspaceResourceId}}",
                    "name": "clv2ws1"
                }
            ]
        },
        "dataFlows": [
            {
                "streams": [
                    "Custom-GCPMonitor"
                ],
                "destinations": [
                    "clv2ws1"
                ],
                "transformKql": "source | extend TimeGenerated = now() | extend resourceDynamic = parse_json(timeSeries[0].resource)| extend resource_type = tostring(resourceDynamic['type'])| extend resource_labels_zone = tostring(resourceDynamic['labels']['zone'])| extend resource_labels_project_id = tostring(resourceDynamic['labels']['project_id'])| extend resource_labels_instance_id = tostring(resourceDynamic['labels']['instance_id'])| extend metricDynamic = parse_json(timeSeries[0].metric)| extend metric_type = tostring(metricDynamic['type'])| extend metric_labels_device_name = tostring(metricDynamic['labels']['device_name'])| extend metric_labels_instance_name = tostring(metricDynamic['labels']['instance_name'])| extend metric_labels_storage_type = tostring(metricDynamic['labels']['storage_type'])| extend metric_labels_device_type = tostring(metricDynamic['labels']['device_type'])| extend value_int64Value = tolong(timeSeries[0].points[0].value.int64Value)| project TimeGenerated, resource_type, resource_labels_zone, resource_labels_project_id, resource_labels_instance_id, metric_type, metric_labels_device_name, metric_labels_instance_name, metric_labels_storage_type, metric_labels_device_type, value_int64Value",
                "outputStream": "Custom-GCP_MONITORINGV2_CL"
            }
        ]
    }
}