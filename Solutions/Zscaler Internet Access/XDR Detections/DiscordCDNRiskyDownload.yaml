schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: 010bd98c-a6be-498c-bdcd-502308c0fdae
    path: Analytic Rules/DiscordCDNRiskyDownload.yaml
    version: 1.0.4
    querySha256: 23137e7c16eb1dd3e28695da81c5d926989aa0acf481eab7263315544f532e1e
    schedule:
      queryFrequency: 1d
      queryPeriod: 1d
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - source queryPeriod has no direct Custom Detection schedule field; verify the KQL lookback
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    errors: []
    originalTactics:
    - CommandAndControl
    originalTechniques:
    - T1071.001
properties:
  id: xdr-discord-cdn-risky-file-download-010bd98c
  displayName: Discord CDN Risky File Download
  status: disabled
  queryCondition:
    queryText: |-
      let connectionThreshold = 1;
      let riskyExtensions = dynamic([".bin",".exe",".dll",".bin",".msi"]);
      CommonSecurityLog
      | where DeviceVendor =~ "ZScaler"
      | where RequestURL has_any("media.discordapp.net", "cdn.discordapp.com")
      | where RequestURL has "attachments"
      | where DeviceAction !~ "blocked"
      | extend DiscordServerId = extract(@"\/attachments\/([0-9]+)\/", 1, RequestURL)
      | summarize dcount(RequestURL), make_set(SourceUserName), make_set(SourceIP), make_set(RequestURL), min(TimeGenerated), max(TimeGenerated), make_set(DeviceAction) by DiscordServerId, DeviceProduct
      | where dcount_RequestURL <= connectionThreshold
      | mv-expand set_SourceUserName to typeof(string), set_RequestURL to typeof(string), set_DeviceAction to typeof(string), set_SourceIP to typeof(string)
      | summarize by DiscordServerId, DeviceProduct, dcount_RequestURL, set_SourceUserName, min_TimeGenerated, max_TimeGenerated, set_DeviceAction, set_SourceIP, set_RequestURL
      | project StartTime=min_TimeGenerated, EndTime=max_TimeGenerated, DeviceActionTaken=set_DeviceAction, DeviceProduct, SourceUser=set_SourceUserName, SourceIP=set_SourceIP, RequestURL=set_RequestURL
      | where RequestURL has_any (riskyExtensions)
  schedule:
    frequency: P1D
  detectionAction:
    alertTemplate:
      title: Discord CDN Risky File Download
      description: '''Identifies callouts to Discord CDN addresses for risky file extensions. This detection will trigger
        when a callout for a risky file is made to a discord server that has only been seen once in your environment. Unique
        discord servers are identified using the server ID that is included in the request URL (DiscordServerId in query).
        Discord CDN has been used in multiple campaigns to download additional payloads'''
      severity: medium
      entityMappings:
        ips:
        - id: ip1
          addressColumn: SourceIP
        urls:
        - id: url1
          addressColumn: RequestURL
      tactics:
      - tactic: CommandAndControl
        techniques:
        - technique: T1071
          subTechniques:
          - T1071.001
