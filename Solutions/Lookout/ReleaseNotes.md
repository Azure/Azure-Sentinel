| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History**                                                 |
|-------------|--------------------------------|--------------------------------------------------------------------|
| 3.0.7       | 13-05-2026                     | Fixed OAuth2 authentication failure for the CCP data connector: added `Authorization: Bearer {api_key}` header to `tokenEndpointHeaders` in the `ResourcesDataConnector` template. Without this header, the Lookout OAuth2 token endpoint (`https://api.lookout.com/oauth2/token`) returned `invalid_client` — "The 'Authorization' header is missing or empty" — preventing the connector from obtaining an access token and causing all polling to fail. Also fixed the same missing `Authorization` header and the `APIKey` double-bracket bug (`]]` → `]`) in the `LookoutStreaming_PollingConfig.json` reference file. Fixed `concat` closing parenthesis typo in `LookoutStreaming_PollingConfig.json` Authorization header value. Fixed ARM-TTK location hardcoding in Playbook resources: replaced `resourceGroup().location` with `parameters('workspace-location')` across all Logic App and API connection resources. Fixed `LookoutEvents` parser field mappings for `SMISHING_ALERT` events: corrected `SmishingAlertId` (`smishing_alert.id` → `smishing_alert.guid`), `SmishingAlertType` and `SmishingAlertCategory` (top-level fields → `smishing_alert.detections[0].alert_type/category`), `SmishingAlertURL` (`smishing_alert.url` → `smishing_alert.detections[0].original_url`), and added `smishing_alert.device.email/guid` as fallback sources for `DeviceEmailAddress`/`DeviceGuid` on smishing events. Fixed `DeviceComplianceStatus` derivation to handle both REST API values (`ACTIVE`/`INACTIVE`) and Streaming API values (`ACTIVATED`/`DEACTIVATED`/`DELETED`). All fixes validated end-to-end against a live Sentinel workspace with 27/27 smoke tests passing across all four event types (THREAT, DEVICE, SMISHING_ALERT, AUDIT). We apologize for the number of iterative updates in this submission — each fix was discovered through hands-on live testing against real data and we wanted to ensure the solution is correct and solid before delivery. |
| 3.0.6       | 07-05-2026                     | Added three Microsoft Sentinel Playbooks (Logic Apps) for automated incident response: **Lookout-DeviceCompliance-Remediation** (enriches device compliance incidents with MDM context and remediation steps), **Lookout-MobileThreat-NotifyAndEnrich** (enriches high/critical mobile threat incidents with threat intelligence and device risk context), **Lookout-SmishingAlert-UserNotify** (enriches smishing/phishing incidents with campaign intelligence and analyst next steps). Updated `Data/Solution_Lookout.json`, `Package/mainTemplate.json`, `Package/createUiDefinition.json`, and `SolutionMetadata.json` to include all three playbooks. Rebuilt package as `3.0.6.zip`. |
| 3.0.5       | 24-04-2026                     | Fixed all 5 workbook metadata `description` fields that contained raw PowerShell serialization artifacts (`System.Object[]`) instead of actual description text — a template generation script bug that produced malformed strings like `@{workbookKey=...; dataTypesDependencies=System.Object[]...}.description`. |
| 3.0.4       | 24-04-2026                     | Fixed `APIKey` bracket escaping in `mainTemplate.json`: changed `[[parameters('applicationKey')]]` to `[[parameters('applicationKey')]` to prevent ARM expression evaluation error (`expected token 'EndOfData' and actual 'RightSquareBracket'`) when Sentinel instantiates the ResourcesDataConnector template. |
| 3.0.3       | 23-04-2026                     | Version bump for certification resubmission. Fixed `workspace-location` parameter `defaultValue` to use `[resourceGroup().location]` ARM expression. |
| 3.0.2       | 11-03-2026                     | Updated `lastPublishDate` across solution metadata and package to 2026-03-11. Cleaned up stale v4.0.0 branches. Resubmission for certification after resolving link discrepancy flagged in Best Practice Test 300.4.1.1. Fixed product branding: updated "Azure Sentinel" to "Microsoft Sentinel" in workbook descriptions. Fixed DCR transform query error: undefined symbol `detections` corrected to `smishing_alert.detections`. Aligned data connector version from `1.0.0` to `3.0.2` for consistent version tracking across all solution components. Updated all template version references from `3.0.1` to `3.0.2` in package. Added **Parsers** and **Notebooks** steps to the install wizard (`createUiDefinition.json`) for improved discoverability during solution deployment. |
| 3.0.1       | 18-12-2025                     | **Parser** v3.1.0 with support for Streaming/Polling/REST API field structures. Enhanced workbooks and dashboards. **Analytic Rules** updated with MITRE ATT&CK mobile tactics. Added **Jupyter Notebooks** for threat hunting: Mobile Malware, Smishing, Device Compliance, and Audit/Insider Threat analysis. |
| 3.0.0       | 07-11-2025                     | New **CCF Connector** added to Solution - *Lookout Mobile Threat Detection Connector*.    |