| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History**                                                 |
|-------------|--------------------------------|--------------------------------------------------------------------|
| 4.0.0       | 03-11-2025                     | **Parser Fix**: Updated parser to support actual v2 API nested JSON structure. Fixed field mappings to extract from `threat.severity`, `threat.device.email`, etc. Added backward compatibility with `coalesce()` for v1/v2 field names. **New Workbooks**: Added Executive Dashboard with KPI metrics and IOA Investigation Dashboard with malicious file hash analysis, smishing detection, device investigation timeline, and threat resolution performance. **Bug Fix**: Fixed APIKey parameter syntax in polling config (missing closing bracket). **Enhancement**: Added support for AUDIT and SMISHING_ALERT event types with proper nested object extraction. |
| 3.0.0       | 18-07-2025                     | New **CCF Connector** added to Solution - *Lookout Mobile Threat Detection Connector*.    |