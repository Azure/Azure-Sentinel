id: 47848010-7a36-44f7-96be-d988f3de9421
kind: Scheduled
name: SAP - Sensitive privileged user makes a change in another user
description: |
  Identifies changes  of sensitive privileged users in other users.

  Source Action: Change user details / authorizations using SU01.

  Priveleged users should be maintained in "SAP - Privileged Users" Watchlist

  This rule requires a fresh full reload of SAP user master data to be performed daily.
  *Data Sources: SAPcon -  Audit Log*
  *Data Sources: SAPcon -  User MD*
severity: High
status: Available
requiredDataConnectors:
  - connectorId: SAPCC
    dataTypes:
      - SAPAuditLog
      - SAPGetSourceIdentifier
      - SAPSystems
      - SAPUsersEmail
      - SAPUsersGetPrivileged
      - SAPUsersGetVIP
queryFrequency: 1h
queryPeriod: 2d
triggerOperator: gt
triggerThreshold: 0
tactics:
  - PrivilegeEscalation
  - CredentialAccess
relevantTechniques: []
query: |
  // SAP - High - Sensitive privileged user makes a change in another user
  // here you can exclude system users which are OK to modify other users' master data
  // by adding those users in the SAP_User_Config watchlist with a tag of 'ChangeUserMasterDataOK'
  let excludeUsersTags= dynamic(['ChangeUserMasterDataOK']);
  let excludedUsers= SAPUsersGetVIP(SearchForTags= excludeUsersTags)| summarize by User2Exclude=SAPUser;
  let SelectedSystemRoles =  dynamic(["Production"]); //can also do// let SelectedSystemRoles =  dynamic(["Production", "UAT"]); dynamic(["All System Roles"])
  let SelectedSystems = SAPSystems(SelectedSystemRoles= SelectedSystemRoles) | project SystemID= SearchKey, SystemRole;
  // time span for audit events
  let AuditTimeAgo= 75m;
  // Audit Log Classes - User Master Changes
  let AuditClasses = dynamic(['AU7', 'AU8', 'AU9', 'AUA', 'AUB', 'AUD', 'BU2']);
  // Query Logic
  SAPAuditLog
  | where TimeGenerated > ago(AuditTimeAgo)
  | where MessageID in (AuditClasses)
  | where SystemID in (SelectedSystems)
  | join kind=leftantisemi excludedUsers on $left.User == $right.User2Exclude
  | join kind= leftsemi SAPUsersGetPrivileged on User, SystemID, $left.ClientID == $right.Client // The user that makes a change is a sensitive privileged user
  | extend ChangedUser= iff(MessageID == "BU2", Variable2, Variable1)
  | lookup  (SAPUsersEmail()
  | project ChangedEmail= Email, ChangedUser= User, SystemID, ClientID)
      on SystemID, ClientID, ChangedUser
  | project
      TimeGenerated,
      Instance,
      SystemID,
      ClientID,
      User,
      MessageText,
      MessageID,
      Email= iff(isempty(Email), User, Email),
      TerminalIPv6,
      Host= iff(isempty(Host), TerminalIPv6, Host),
      ChangedUser,
      ChangedEmail
  | extend AlertRuleUniqueName = 'sensitiveprivilegedusermakesachangeinanotheruser'
  | extend SourceIdentifier= trim("-", strcat_delim("-","SAP", column_ifexists("SystemID", ""), column_ifexists("ClientID", ""), toscalar(SAPGetSourceIdentifier() | project SourceIdentifier)))
eventGroupingSettings:
  aggregationKind: AlertPerResult
entityMappings:
  - entityType: Account
    fieldMappings:
      - identifier: FullName
        columnName: Email
  - entityType: Host
    fieldMappings:
      - identifier: FullName
        columnName: Host
  - entityType: IP
    fieldMappings:
      - identifier: Address
        columnName: TerminalIPv6
  - entityType: CloudApplication
    fieldMappings:
      - identifier: Name
        columnName: SourceIdentifier
  - entityType: Account
    fieldMappings:
      - identifier: FullName
        columnName: ChangedEmail
customDetails:
  AlertRuleUniqueName: AlertRuleUniqueName
  ChangedUser: ChangedUser
  SAP_User: User
version: 3.5.09
