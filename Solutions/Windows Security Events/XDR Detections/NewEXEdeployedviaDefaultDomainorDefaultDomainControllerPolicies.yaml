schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: 05b4bccd-dd12-423d-8de4-5a6fb526bb4f
    path: Analytic Rules/NewEXEdeployedviaDefaultDomainorDefaultDomainControllerPolicies.yaml
    version: 1.0.2
    querySha256: d7636b33d4a545b02c3bf662b4d8e4df8ca08aced50e6ef136f02fc7798a7153
    schedule:
      queryFrequency: 1d
      queryPeriod: 14d
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - Host.FullName has no supported Custom Detection field
    - multiple source tactics were resolved by the configured per-rule override
    errors: []
    originalTactics:
    - Execution
    - LateralMovement
    originalTechniques:
    - T1072
    - T1570
properties:
  id: xdr-new-exe-deployed-via-default-domain-or-default-domain-controller-policie-05b4bccd
  displayName: New EXE deployed via Default Domain or Default Domain Controller Policies
  status: disabled
  queryCondition:
    queryText: |-
      let known_processes = (
        SecurityEvent
        // If adjusting Query Period or Frequency update these
        | where TimeGenerated between(ago(14d)..ago(1d))
        | where EventID == 4688
        | where NewProcessName has_any ("Policies\\{6AC1786C-016F-11D2-945F-00C04fB984F9}", "Policies\\{31B2F340-016D-11D2-945F-00C04FB984F9}")
        | summarize by Process);
        SecurityEvent
        // If adjusting Query Period or Frequency update these
        | where TimeGenerated > ago(1d)
        | where EventID == 4688
        | where NewProcessName has_any ("Policies\\{6AC1786C-016F-11D2-945F-00C04fB984F9}", "Policies\\{31B2F340-016D-11D2-945F-00C04FB984F9}")
        | where Process !in (known_processes)
        // This will likely apply to multiple hosts so summarize these data
        | summarize FirstSeen=min(TimeGenerated), LastSeen=max(TimeGenerated) by Process, NewProcessName, CommandLine, Computer
        | extend HostName = tostring(split(Computer, ".")[0]), DomainIndex = toint(indexof(Computer, '.'))
        | extend HostNameDomain = iff(DomainIndex != -1, substring(Computer, DomainIndex + 1), Computer)
  schedule:
    frequency: P1D
  detectionAction:
    alertTemplate:
      title: New EXE deployed via Default Domain or Default Domain Controller Policies
      description: |-
        'This detection highlights executables deployed to hosts via either the Default Domain or Default Domain Controller Policies. These policies apply to all hosts or Domain Controllers and best practice is that these policies should not be used for deployment of files.
        A threat actor may use these policies to deploy files or scripts to all hosts in a domain.'
      severity: high
      entityMappings:
        hosts:
        - id: host1
          nameColumn: HostName
          dnsDomainColumn: HostNameDomain
      tactics:
      - tactic: LateralMovement
        techniques:
        - technique: T1570
