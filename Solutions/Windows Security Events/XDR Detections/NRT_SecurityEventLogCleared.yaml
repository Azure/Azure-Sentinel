schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: 508cef41-2cd8-4d40-a519-b04826a9085f
    path: Analytic Rules/NRT_SecurityEventLogCleared.yaml
    version: 1.0.1
    querySha256: ec40f4d8c3a9c6d43056458d5848e2cef5bbfc0d2eb14f33c09438e9ceeb4eef
    schedule:
      queryFrequency: ''
      queryPeriod: ''
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - Account.NTDomain column `AccountNTDomain` is not produced by the query
    - Host.FullName has no supported Custom Detection field
    - Unconfirmed generic Account.Name mapping was omitted because another required Host, Mailbox, or IP asset is available
    errors: []
    originalTactics:
    - DefenseEvasion
    originalTechniques:
    - T1070
properties:
  id: xdr-nrt-security-event-log-cleared-508cef41
  displayName: NRT Security Event log cleared
  status: disabled
  queryCondition:
    queryText: |-
      SecurityEvent
      | where EventID == 1102 and EventSourceName == "Microsoft-Windows-Eventlog"
      | summarize StartTimeUtc = min(TimeGenerated), EndTimeUtc = max(TimeGenerated), EventCount = count() by Computer, Account, EventID, Activity
      | extend HostName = tostring(split(Computer, ".")[0]), DomainIndex = toint(indexof(Computer, '.'))
      | extend HostNameDomain = iff(DomainIndex != -1, substring(Computer, DomainIndex + 1), Computer)
      | extend AccountName = tostring(split(Account, @'\')[1]), AccountNTDomain = tostring(split(Account, @'\')[0])
  schedule:
    frequency: PT1H
  detectionAction:
    alertTemplate:
      title: NRT Security Event log cleared
      description: |-
        'Checks for event id 1102 which indicates the security event log was cleared.
        It uses Event Source Name "Microsoft-Windows-Eventlog" to avoid generating false positives from other sources, like AD FS servers for instance.'
      severity: medium
      entityMappings:
        hosts:
        - id: host1
          nameColumn: HostName
          dnsDomainColumn: HostNameDomain
      tactics:
      - tactic: DefenseEvasion
        techniques:
        - technique: T1070
