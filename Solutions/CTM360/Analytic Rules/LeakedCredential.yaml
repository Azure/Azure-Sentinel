id: a0a46e91-3f94-4ed4-ab70-ecd36ae0ead0
name: Leaked Credential
description: |
  'New Leaked Credential with severity High found'
severity: High
status: Available
requiredDataConnectors:
  - connectorId: CTM360CBSConnectorDefinition
    dataTypes:
      - CBS_BreachedCredentials
queryFrequency: 5m
queryPeriod: 5m
triggerOperator: gt
triggerThreshold: 0
tactics:
  - CredentialAccess
relevantTechniques:
  - T1555
query: |+
  CBS_BreachedCredentials_CL | where Severity == "High" | where IncidentType == "Leaked Credential" | where Status != "Closed" or Status != "Resolved" 
suppressionEnabled: false
incidentConfiguration:
  createIncident: true
  groupingConfiguration:
    enabled: false
    reopenClosedIncident: false
    lookbackDuration: PT5H
    matchingMethod: AllEntities
eventGroupingSettings:
  aggregationKind: SingleAlert
suppressionDuration: 5h
version: 1.0.4
kind: Scheduled

