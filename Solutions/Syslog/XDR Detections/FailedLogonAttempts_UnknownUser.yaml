schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: e7ec9fa6-e7f7-41ed-a34b-b956837a3ee6
    path: Analytic Rules/FailedLogonAttempts_UnknownUser.yaml
    version: 1.0.5
    querySha256: f05d75464d053f17049485947f0c87326885c6790cd9072e2cfd7e313491f9ce
    schedule:
      queryFrequency: 1d
      queryPeriod: 1d
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - source queryPeriod has no direct Custom Detection schedule field; verify the KQL lookback
    - Host.FullName has no supported Custom Detection field
    errors: []
    originalTactics:
    - CredentialAccess
    originalTechniques:
    - T1110
properties:
  id: xdr-failed-logon-attempts-in-authpriv-e7ec9fa6
  displayName: Failed logon attempts in authpriv
  status: disabled
  queryCondition:
    queryText: |-
      let threshold = 15;
      // Below pulls messages from syslog-authpriv logs where there was an authentication failure with an unknown user.
      // IP address of system attempting logon is also extracted from the SyslogMessage field. Some of these messages
      // are aggregated.
      Syslog
      | where Facility =~ "authpriv"
      | where SyslogMessage has "authentication failure" and SyslogMessage has " uid=0"
      | extend RemoteIP = extract(@".*?rhost=([\d.]+).*?", 1,SyslogMessage)
      | project TimeGenerated, Computer, ProcessName, HostIP, RemoteIP, ProcessID
      | join kind=innerunique (
          // Below pulls messages from syslog-authpriv logs that show each instance an unknown user tried to logon.
          Syslog
          | where Facility =~ "authpriv"
          | where SyslogMessage has "user unknown"
          | project Computer, HostIP, ProcessID
          ) on Computer, HostIP, ProcessID
      // Count the number of failed logon attempts by External IP and internal machine
      | summarize FirstLogonAttempt = min(TimeGenerated), LatestLogonAttempt = max(TimeGenerated), TotalLogonAttempts = count() by Computer, HostIP, RemoteIP
      // Calculate the time between first and last logon attempt (AttemptPeriodLength)
      | extend TimeBetweenLogonAttempts = LatestLogonAttempt - FirstLogonAttempt
      | where TotalLogonAttempts >= threshold
      | project FirstLogonAttempt, LatestLogonAttempt, TimeBetweenLogonAttempts, TotalLogonAttempts, SourceAddress = RemoteIP, Computer,  HostIP
      | sort by Computer asc nulls last
  schedule:
    frequency: P1D
  detectionAction:
    alertTemplate:
      title: Failed logon attempts in authpriv
      description: "'Identifies failed logon attempts from unknown users in Syslog authpriv logs. The unknown user means the\
        \ account that tried to log in isn't provisioned on the machine. A few hits could indicate someone attempting to access\
        \ a machine they aren't authorized to access. \nIf there are many of hits, especially from outside your network, it\
        \ could indicate a brute force attack. \nDefault threshold for logon attempts is 15.'"
      severity: medium
      entityMappings:
        ips:
        - id: ip1
          addressColumn: HostIP
      tactics:
      - tactic: CredentialAccess
        techniques:
        - technique: T1110
