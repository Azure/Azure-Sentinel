| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History**                          |
|-------------|--------------------------------|---------------------------------------------|
| 3.0.5       | 10-07-2026                     | DCR KQL transforms route logs to standard tables (Syslog, WindowsEvent, SecurityEvent, ASimDnsActivityLogs, ASimWebSessionLogs, ASimNetworkSessionLogs, ABAPAuditLog and more). Windows Security events parsed into native SecurityEvent columns (event IDs 4688, 4624, 4648, 4719, 4625) and Sysmon/PowerShell events packed into WindowsEvent EventData (Sysmon 1,3,11,12,13,14,22,23,26 and PowerShell 4103/4104). Three Web Dispatcher access-log formats supported (bracketed-header, `… sent … ms to …`, and the `t0(timeofday):` performance format) plus ABAP ICM HTTP access logs (abap/httpaccesslog) to ASimWebSessionLogs (typed as WebServerSession with a path-only URL). Squid proxy access logs to ASimWebSessionLogs now also populate DstHostname/DstFQDN from the request URL, with `-` src/dest IPs normalized to empty strings for ASIM compliance. HANA audit severity mapped to Syslog severity; Syslog message stripped of duplicated timestamp/host/tag; DNS bind response events parsed correctly. Catch-all retains any unmatched/future source into SAPLogServ_CL instead of dropping it; HANA DB analytic rules read from the Syslog table instead of SAPLogServ_CL; connector description clarifies ASIM/standard-table detections are covered out of the box and not duplicated here; retained SAPLogServ_CL test/test1 columns for upgrade compatibility |
| 3.0.4       |  11-08-2025                    | Connector UI updates |
| 3.0.3       |  17-07-2025                    | Observability Workbook added |
| 3.0.2       |  25-06-2025                    | Analytic Rules for HANA DB added |
| 3.0.1       |  09-04-2025                    | Retention setting dropped from table to default to LogAnalytics ws default |
| 3.0.0       |  17-02-2025                    | Initial Solution Release |
