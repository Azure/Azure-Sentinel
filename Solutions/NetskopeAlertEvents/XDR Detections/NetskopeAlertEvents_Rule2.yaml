schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: b2e7d3c5-9c46-4f2a-8d1b-3e5f7a9c2d41
    path: Analytic Rules/NetskopeAlertEvents_Rule2.yaml
    version: 1.0.0
    querySha256: 0d25a3d0aa4e96b8b109876f24c1699691769916c4dfed1806fb5bd27136e334
    schedule:
      queryFrequency: 1h
      queryPeriod: 1h
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - Unconfirmed generic Account.Name mapping was omitted because another required Host, Mailbox, or IP asset is available
    - multiple source tactics were resolved by the configured per-rule override
    errors: []
    originalTactics:
    - Exfiltration
    - CommandAndControl
    originalTechniques:
    - T1567
    - T1102
properties:
  id: xdr-netskope-suspicious-application-activity-low-confidence-risky-app-b2e7d3c5
  displayName: Netskope - Suspicious Application Activity (Low Confidence / Risky App)
  status: disabled
  queryCondition:
    queryText: |-
      let riskyCcl = dynamic(["low", "poor"]);
      let sensitiveActivities = dynamic(["Upload", "Share", "Download", "Post", "Send"]);
      NetskopeAlertEvents_CL
      | where TimeGenerated > ago(1h)
      | where isnotempty(App)
      | where tolower(Ccl) in (riskyCcl)
           or Action =~ "block"
           or (Activity in~ (sensitiveActivities) and tolower(Ccl) != "excellent")
      | summarize
          EventCount = count(),
          Activities = make_set(Activity, 20),
          Actions = make_set(Action, 10),
          CCLs = make_set(Ccl, 10),
          Categories = make_set(Appcategory, 20),
          DistinctApps = dcount(App),
          Apps = make_set(App, 20),
          LastSeen = max(TimeGenerated)
          by User, Userip, Hostname, DeviceClassification
      | where EventCount > 5 or DistinctApps > 3
      | extend RiskIndicators = strcat_array(array_concat(
          iff(set_has_element(CCLs, "low") or set_has_element(CCLs, "poor"), dynamic(["Low Confidence App"]), dynamic([])),
          iff(set_has_element(Actions, "block"), dynamic(["Blocked Activity"]), dynamic([])),
          iff(DistinctApps > 3, dynamic(["Multiple Risky Apps"]), dynamic([]))
      ), ", ")
      | order by EventCount desc
      | project
          LastSeen,
          User,
          Userip,
          Hostname,
          DeviceClassification,
          EventCount,
          DistinctApps,
          Apps,
          Categories,
          Activities,
          Actions,
          CCLs,
          RiskIndicators
  schedule:
    frequency: PT1H
  detectionAction:
    alertTemplate:
      title: Netskope - Suspicious Application Activity (Low Confidence / Risky App)
      description: |-
        Detects activity involving risky or low Cloud Confidence Level (CCL) applications,
        blocked application actions, or sensitive activities (upload, share, download) on
        unsanctioned apps. Helps surface Shadow IT and potential data leakage via risky
        cloud applications.
      severity: medium
      entityMappings:
        ips:
        - id: ip1
          addressColumn: Userip
        hosts:
        - id: host1
          nameColumn: Hostname
      tactics:
      - tactic: Exfiltration
        techniques:
        - technique: T1567
