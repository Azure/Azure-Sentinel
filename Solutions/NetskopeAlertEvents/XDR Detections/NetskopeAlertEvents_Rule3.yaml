schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: c3f8e4d6-0d57-4a3b-9e2c-4f6a8b0d3e52
    path: Analytic Rules/NetskopeAlertEvents_Rule3.yaml
    version: 1.0.0
    querySha256: 6da47fa464601eb32c355e5afd37b6f94ee97dfbb4dd6b8d83267acf6ee5e017
    schedule:
      queryFrequency: 1h
      queryPeriod: 1h
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - Unconfirmed generic Account.Name mapping was omitted because another required Host, Mailbox, or IP asset is available
    - multiple source tactics were resolved by the configured per-rule override
    errors: []
    originalTactics:
    - Exfiltration
    - Collection
    originalTechniques:
    - T1567
    - T1530
properties:
  id: xdr-netskope-dlp-incident-spike-c3f8e4d6
  displayName: Netskope - DLP Incident Spike
  status: disabled
  queryCondition:
    queryText: |-
      let dlpIncidentThreshold = 10;
      NetskopeAlertEvents_CL
      | where TimeGenerated > ago(1h)
      | where AlertType =~ "DLP" or isnotempty(DlpProfile) or isnotempty(DlpIncidentId)
      | summarize
          IncidentCount = count(),
          DistinctIncidents = dcount(DlpIncidentId),
          Profiles = make_set(DlpProfile, 20),
          Rules = make_set(DlpRule, 20),
          Applications = make_set(App, 20),
          Files = make_set(DlpFile, 20),
          Activities = make_set(Activity, 10),
          Actions = make_set(Action, 10),
          FirstSeen = min(TimeGenerated),
          LastSeen = max(TimeGenerated)
          by User, Userip, Hostname
      | where IncidentCount > dlpIncidentThreshold
      | order by IncidentCount desc
      | project
          LastSeen,
          User,
          Userip,
          Hostname,
          IncidentCount,
          DistinctIncidents,
          Profiles,
          Rules,
          Applications,
          Files,
          Activities,
          Actions,
          FirstSeen
  schedule:
    frequency: PT1H
  detectionAction:
    alertTemplate:
      title: Netskope - DLP Incident Spike
      description: |-
        Detects a spike in Netskope DLP incidents within a short window. A sudden increase in
        DLP violations for a single user or DLP profile can indicate active data exfiltration,
        a misconfigured policy, or bulk handling of sensitive data. Triggers when a user
        generates more DLP incidents in the last hour than a configurable threshold.
      severity: high
      entityMappings:
        ips:
        - id: ip1
          addressColumn: Userip
        hosts:
        - id: host1
          nameColumn: Hostname
      tactics:
      - tactic: Exfiltration
        techniques:
        - technique: T1567
