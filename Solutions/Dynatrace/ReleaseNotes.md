| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History**                          |
|-------------|--------------------------------|---------------------------------------------|
| 3.0.4       | 30-06-2026                     | Added **DynatraceRuntimeVulnerabilitiesV3**, **DynatraceAttacksV3**, **DynatraceAuditLogsV3**, and **DynatraceProblemsV3** data connectors using the Dynatrace Grail DQL Storage Query API (`/platform/storage/query/v1/query:execute`) with platform token authentication. Vulnerabilities connector uses `storage:security.events:read` scope and returns a current-state snapshot with exposure status, exploit availability, vulnerable function usage, and data asset reachability. Attacks connector uses `storage:security.events:read` scope and fetches `DETECTION_FINDING` events with a 2-hour rolling window. Audit Logs connector uses `storage:system.events:read` scope and fetches `AUDIT_EVENT` records with a 30-minute rolling window. Problems connector uses `storage:davis.problems:read` scope and fetches Davis AI problem records with a 2-hour rolling window including problem name, category, severity, impact level, root cause entity, affected entities, mute status, and maintenance window flags. Updated **DynatraceSecurityProblems** (v3.0.0), **DynatraceAttacks** (v3.0.0), **DynatraceAuditLogs** (v3.0.0), and **DynatraceProblems** (v3.0.0) parsers to union V1, V2, and V3 table sources. Marked **DynatraceRuntimeVulnerabilitiesV2**, **DynatraceAttacksV2**, **DynatraceAuditLogsV2**, and **DynatraceProblemsV2** connectors as GA. Added **Add_DynatraceApplicationSecurityAttackSourceIpSTIXThreatIntelligence** playbook as a replacement for the deprecated tiIndicators-based playbook, using the Microsoft Sentinel STIX 2.1 threat intelligence upload API. |
| 3.0.3       | 28-04-2026                     | Fixed timestamp type mismatch in **Parsers** (DynatraceAttacks, DynatraceAuditLogs, DynatraceProblems, DynatraceSecurityProblems): V1 Unix epoch millisecond fields now converted to datetime, resolving duplicate typed columns in query results. Marked **Add_DynatraceApplicationSecurityAttackSourceIpThreatIntelligence** playbook as deprecated due to the deprecation of the Microsoft Graph Security tiIndicators API and its connector in Logic Apps. |
| 3.0.2       | 02-04-2026                     | Added DCR based connectors.                 |
| 3.0.1       | 18-01-2024                     | Changes for rebranding from Microsoft 365 Defender to Microsoft Defender XDR, Updated user-agent strings used when calling Dynatrace REST API's, Added new Entity Mappings to **Analytic Rules**                                    Aligned Playbook, Data Connector & Workbook version numbers with rest of solution.           |
| 3.0.0       | 16-10-2023                     | Enabled new api paging mode on **Data Connector** to fix issues related to polling Dynatrace REST API's with a large number of results.     |
