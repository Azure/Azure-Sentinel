| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History**                     |
|------------|--------------------------------|----------------------------------------|
| 3.0.0      | 26-04-2026                     | Migrated connector from Azure Functions to Codeless Connector Framework (CCF/CCP); existing `Guardicore*_CL` tables must be upgraded via `az monitor log-analytics workspace table migrate`; Workspace shared-key permission no longer required. |
| 3.1.0      | 27-04-2026                     | Added incident-enrichment playbook (`Guardicore-ProcessIncidentEnrichment`) and companion recurrence-triggered runner (`Guardicore-EnrichmentRunner`) implemented natively in Logic Apps - no Azure Function App. Writes to `GuardicoreProcessedIncidents_CL` and `GuardicoreEnrichingConnections_CL` via the Logs Ingestion API to a Data Collection Rule using system-assigned managed identity (no workspace shared key). Greenfield release: the legacy companion Function App at `Playbooks/CustomConnector/` is removed; deploy the DCR + Tables + Storage prereq from `Playbooks/AkamaiGuardicoreEnrichment_dcr/` before deploying the playbooks. |
