id: 9ddbd09d-6119-4e78-bf15-911c4113d608
name: Microsoft Teams External versus Internal Thread Exposure
description: |
  This query shows de-duplicated Microsoft Teams messages split by whether the thread includes external participants.
description-detailed: |
  This query shows de-duplicated Microsoft Teams messages split by whether the thread includes external participants, using Advance hunting in Microsoft Defender XDR. External threads carry the higher risk of social-engineering and malicious content.
requiredDataConnectors:
- connectorId: MicrosoftThreatProtection
  dataTypes:
  - MessageEvents
tactics:
  - InitialAccess
relevantTechniques:
  - T1566
query: |
  //This query shows de-duplicated Microsoft Teams messages split by whether the thread includes external participants.
  MessageEvents
  | where Timestamp > ago(30d)
  | summarize arg_max(Timestamp, *) by TeamsMessageId
  | extend Thread = iif(IsExternalThread == true, "External Thread", "Internal Thread")
  | summarize Count = count() by Thread
  | render piechart
version: 1.0.0
