id: 6f2d71d6-e6c4-0da4-91da-e8192dc5b12c
name: "UniFi Cloud: ISP Packet Loss"
description: |
  Detects when packet loss is occurring on the WAN connection. Even small amounts of packet loss can significantly impact application performance, VoIP calls, and video conferencing.
severity: Medium
status: Available
requiredDataConnectors:
  - connectorId: UniFiSiteManagerConnectorDefinition
    dataTypes:
      - Unifi_Cloud_ISPMetrics_CL
queryFrequency: PT15M
queryPeriod: PT30M
triggerOperator: GreaterThan
triggerThreshold: 0
tactics:
  - Impact
relevantTechniques:
  - T1498
  - T1499
query: |
  // UniFi ISP Packet Loss Detection
  // Author: Fetch Labs (https://sentinel.blog)
  let PacketLossThreshold = 1;
  Unifi_Cloud_ISPMetrics_CL
  | where TimeGenerated > ago(30m)
  | mv-expand period = periods
  | extend 
      metricTime = todatetime(period.metricTime),
      packetLoss = toint(period.data.wan.packetLoss),
      ispName = tostring(period.data.wan.ispName),
      ispAsn = tostring(period.data.wan.ispAsn),
      avgLatency = toint(period.data.wan.avgLatency)
  | where packetLoss >= PacketLossThreshold
  | summarize 
      TotalPacketLoss = sum(packetLoss),
      EventCount = count(),
      AvgLatency = avg(avgLatency),
      FirstSeen = min(metricTime),
      LastSeen = max(metricTime)
      by siteId, ispName, ispAsn
  | extend TimeGenerated = now()
  | project
      TimeGenerated,
      SiteId = siteId,
      ISPName = ispName,
      ISPAsn = ispAsn,
      TotalPacketLoss,
      EventCount,
      AvgLatencyMs = round(AvgLatency, 1),
      FirstSeen,
      LastSeen
entityMappings:
  - entityType: CloudApplication
    fieldMappings:
      - identifier: Name
        columnName: ISPName
incidentConfiguration:
  createIncident: true
  groupingConfiguration:
    enabled: true
    reopenClosedIncident: false
    lookbackDuration: PT4H
    matchingMethod: AllEntities
    groupByEntities: []
kind: Scheduled
version: 1.0.0
