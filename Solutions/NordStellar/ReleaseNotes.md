| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History**                                                                                                                                                                                                                                                                       |
|-------------|--------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 3.0.1       | 19-08-2026                     | `TimeGenerated` is now set to ingestion time so historical events (e.g. old `date_added` values) are no longer rejected by the Log Analytics ingestion-time retention check. The original event timestamp (`detected_at`/`date_added`) is preserved in a new `EventTime` column. Fixed `AssetType` for `CONSUMER_CREDENTIAL` events: it is now `DOMAIN` instead of the credential origin (`MALWARE_INFECTION`/`COMBO_LIST`). |
| 3.0.0       | 27-05-2026                     | Initial release of the NordStellar (Push) connector built on the Codeless Connector Framework. Adds a unified `NordStellar_CL` table, a Data Collection Rule with KQL transform, OAuth 2.0 client-credentials authentication, and support for all NordStellar event types across the Leaked Data, Dark Web Monitoring, Domain Squatting, and Attack Surface modules. |
