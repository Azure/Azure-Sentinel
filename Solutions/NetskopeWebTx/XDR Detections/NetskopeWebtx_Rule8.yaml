schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: dacab67e-fcf3-41c6-a191-579c7be1814d
    path: Analytic Rules/NetskopeWebtx_Rule8.yaml
    version: 1.0.0
    querySha256: 5f7d3487b9d5704b03fdec4d87ed48ea0cc498b75ba10daa1ba637a0c968cc3d
    schedule:
      queryFrequency: 1h
      queryPeriod: 1h
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - 'Entity confirmation required: `User` was provisionally mapped to Account.upnColumn. Confirm that the source always
      emits a complete UPN; otherwise project an Entra user ID, SID, or account name plus domain'
    - multiple source tactics were resolved by the configured per-rule override
    errors: []
    originalTactics:
    - DefenseEvasion
    - Exfiltration
    originalTechniques:
    - T1562
    - T1048
    acceptedReviewReasons:
    - 'Entity confirmation required: `User` was provisionally mapped to Account.upnColumn. Confirm that the source always
      emits a complete UPN; otherwise project an Entra user ID, SID, or account name plus domain'
properties:
  id: xdr-netskope-repeated-or-critical-policy-violations-dacab67e
  displayName: Netskope - Repeated or Critical Policy Violations
  status: disabled
  queryCondition:
    queryText: |-
      let violationThreshold = 5;
      NetskopeWebTransactions_CL
      | where TimeGenerated > ago(1h)
      | where isnotempty(CsUsername)
      | where isnotempty(XPolicyAction) and isnotempty(XPolicyName)
      | where XPolicyAction =~ 'block' or XPolicyAction =~ 'alert'
      | summarize
          ViolationCount = count(),
          Policies = make_set(XPolicyName),
          PolicyActions = make_set(XPolicyAction),
          Apps = make_set(XCsApp),
          Hosts = make_set(CsHost),
          Activities = make_set(XCsAppActivity),
          BlockCount = countif(XPolicyAction =~ 'block'),
          AlertCount = countif(XPolicyAction =~ 'alert'),
          FirstViolation = min(TimeGenerated),
          LastViolation = max(TimeGenerated)
          by CsUsername, XCCountry, XCDevice
      | where ViolationCount >= violationThreshold or BlockCount > 0
      | extend ViolationSeverity = case(
          BlockCount > 10, 'Critical',
          BlockCount > 5, 'High',
          ViolationCount > 20, 'High',
          ViolationCount > 10, 'Medium',
          'Low')
      | project
          TimeGenerated = LastViolation,
          User = CsUsername,
          TotalViolations = ViolationCount,
          BlockedAttempts = BlockCount,
          AlertedActions = AlertCount,
          ViolatedPolicies = Policies,
          PolicyActions,
          ApplicationsInvolved = Apps,
          TargetHosts = Hosts,
          Activities,
          Country = XCCountry,
          Device = XCDevice,
          ViolationSeverity,
          FirstOccurrence = FirstViolation,
          LastOccurrence = LastViolation
  schedule:
    frequency: PT1H
  detectionAction:
    alertTemplate:
      title: Netskope - Repeated or Critical Policy Violations
      description: Detects users with repeated policy violations or critical policy blocks. Monitors policy enforcement effectiveness
        and compliance.
      severity: high
      entityMappings:
        accounts:
        - id: account1
          upnColumn: User
      tactics:
      - tactic: DefenseEvasion
        techniques:
        - technique: T1562
