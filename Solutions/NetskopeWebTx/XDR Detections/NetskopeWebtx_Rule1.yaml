schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: fa4c4f1c-3c5f-4c3a-a13f-924c30db56e9
    path: Analytic Rules/NetskopeWebtx_Rule1.yaml
    version: 1.0.0
    querySha256: 10d4be4d7ae61cf9b2ddb8892866da4cd5e94879a159d513327dd6475ff7e07e
    schedule:
      queryFrequency: 1h
      queryPeriod: 1h
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - Unconfirmed generic Account.Name mapping was omitted because another required Host, Mailbox, or IP asset is available
    - multiple source tactics were resolved by the configured per-rule override
    errors: []
    originalTactics:
    - Exfiltration
    - Collection
    originalTechniques:
    - T1567
    - T1074
properties:
  id: xdr-netskope-anomalous-user-behavior-high-volume-from-unmanaged-device-fa4c4f1c
  displayName: Netskope - Anomalous User Behavior (High Volume from Unmanaged Device)
  status: disabled
  queryCondition:
    queryText: |-
      let highVolumeThresholdGB = 1;
      NetskopeWebTransactions_CL
      | where TimeGenerated > ago(1h)
      | where isnotempty(CsUsername)
      | summarize
          TotalBytes = sum(Bytes),
          UploadBytes = sum(CsBytes),
          DownloadBytes = sum(ScBytes),
          UniqueApps = dcount(XCsApp),
          Apps = make_set(XCsApp, 20),
          UniqueHosts = dcount(CsHost),
          Activities = make_set(XCsAppActivity),
          Countries = make_set(XCCountry),
          Devices = make_set(XCDevice),
          AccessMethods = make_set(XCsAccessMethod),
          EventCount = count()
          by CsUsername, XCDevice, XCsAccessMethod
      | extend
          TotalGB = round(TotalBytes / 1073741824.0, 3),
          UploadGB = round(UploadBytes / 1073741824.0, 3),
          DownloadGB = round(DownloadBytes / 1073741824.0, 3)
      | where TotalGB > highVolumeThresholdGB
      | extend IsUnmanagedDevice = XCDevice =~ 'unmanaged' or XCDevice =~ 'BYOD' or XCDevice =~ 'Personal' or XCDevice =~ 'Unknown' or XCsAccessMethod != 'Client'
      | where IsUnmanagedDevice or TotalGB > 5 or UniqueApps > 20
      | extend RiskIndicators = strcat_array(array_concat(
          iff(IsUnmanagedDevice, dynamic(['Unmanaged Device']), dynamic([])),
          iff(TotalGB > 5, dynamic(['High Data Volume']), dynamic([])),
          iff(UniqueApps > 20, dynamic(['Many Apps Accessed']), dynamic([])),
          iff(array_length(Countries) > 1, dynamic(['Multiple Countries']), dynamic([]))
      ), ', ')
      | project
          TimeGenerated = now(),
          User = CsUsername,
          Device = XCDevice,
          AccessMethod = XCsAccessMethod,
          TotalDataGB = TotalGB,
          UploadGB,
          DownloadGB,
          UniqueApplications = UniqueApps,
          Applications = Apps,
          UniqueHosts,
          Activities,
          Countries,
          EventCount,
          IsUnmanagedDevice,
          RiskIndicators
  schedule:
    frequency: PT1H
  detectionAction:
    alertTemplate:
      title: Netskope - Anomalous User Behavior (High Volume from Unmanaged Device)
      description: Detects anomalous user behavior including high data volume transfers from unmanaged devices, unusual access
        patterns, and suspicious application usage.
      severity: medium
      entityMappings:
        hosts:
        - id: host1
          nameColumn: Device
      tactics:
      - tactic: Exfiltration
        techniques:
        - technique: T1567
