schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: 849ea271-cd9c-4afe-a13b-ddbbac5fc6d3
    path: Analytics Rules/1Password - User added to privileged group.yaml
    version: 1.0.0
    querySha256: 6ea83a210e23be0c22345b5a67c90931ba26d7ff9c3c53ae0a8cc1bb1cb0def5
    schedule:
      queryFrequency: 5m
      queryPeriod: 5m
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - source queryPeriod has no direct Custom Detection schedule field; verify the KQL lookback
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    errors: []
    originalTactics:
    - Persistence
    originalTechniques:
    - T1098
properties:
  id: xdr-1password-user-added-to-privileged-group-849ea271
  displayName: 1Password - User added to privileged group
  status: disabled
  queryCondition:
    queryText: |-
      let watchlist =
          _GetWatchlist("PG1PW")
          | project SearchKey
      ;
      let groups = dynamic([""]);
      OnePasswordEventLogs_CL
      | where log_source == "auditevents"
      | where action == "join"
      | where object_type == "gm"
      | where tostring(actor_details.email) != tostring(aux_details.email)
      // Enable the line below when using the "Privileged Groups - 1PW" watchlist
      | where object_uuid in (watchlist)
      // Enable the line below when using the dynamic groups list within the analytics rule itself
      // | where object_uuid in (groups)
      | extend
          TargetUsername = aux_details.email
          , ActorUsername = actor_details.email
          , SrcIpAddr = session.ip
          , GroupRole = case(
              aux_info == "R", "Group member"
              , aux_info == "A", "Group manager"
              , aux_info
          )
  schedule:
    frequency: PT5M
  detectionAction:
    alertTemplate:
      title: 1Password - User added to privileged group
      description: |-
        This will alert when a user is added to a privileged group which has been implemented by an actor that was not the target user account. Once the analytics rule is triggered it will group all related future alerts for upto 30 minutes when all related entities are the same.

        Ref: https://1password.com/
        Ref: https://github.com/securehats/
      severity: medium
      entityMappings:
        ips:
        - id: ip1
          addressColumn: SrcIpAddr
      tactics:
      - tactic: Persistence
        techniques:
        - technique: T1098
