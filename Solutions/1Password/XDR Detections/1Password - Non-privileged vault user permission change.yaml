schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: 327e0579-7c03-4ec7-acf5-a29dcc4a12b6
    path: Analytics Rules/1Password - Non-privileged vault user permission change.yaml
    version: 1.0.0
    querySha256: e60d719907f3015915a3ffcb12679e86813e70e9e8598b3227338ef8cec626e5
    schedule:
      queryFrequency: 5m
      queryPeriod: 5m
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - source queryPeriod has no direct Custom Detection schedule field; verify the KQL lookback
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    errors: []
    originalTactics:
    - Persistence
    originalTechniques:
    - T1098
properties:
  id: xdr-1password-non-privileged-vault-user-permission-change-327e0579
  displayName: 1Password - Non-privileged vault user permission change
  status: disabled
  queryCondition:
    queryText: |-
      let watchlist =
          _GetWatchlist("PV1PW")
          | project SearchKey
      ;
      // Insert the vault UUIDs below when using the dynamic vaults list within the analytics rule itself
      let vaults = dynamic([""]);
      OnePasswordEventLogs_CL
      | where log_source == "auditevents"
      | where action has_any("grant", "revoke", "update")
      | where object_type == "uva"
      | where tostring(actor_details.email) != tostring(aux_details.email)
      // Enable the line below when using the "Privileged Vaults - 1PW" watchlist
      | where object_uuid !in (watchlist)
      // Enable the line below when using the dynamic vaults list within the analytics rule itself
      // | where object_uuid !in (vaults)
      | extend
          TargetUsername = aux_details.email
          , ActorUsername = actor_details.email
          , SrcIpAddr = session.ip
  schedule:
    frequency: PT5M
  detectionAction:
    alertTemplate:
      title: 1Password - Non-privileged vault user permission change
      description: |-
        This will alert when user permissions have changed within a non-privileged vault which have been implemented by an actor that was not the target user account. Once this analytics rule is triggered it will group all related future alerts for upto an hour when all related entities are the same.

        Ref: https://1password.com/
        Ref: https://github.com/securehats/
      severity: medium
      entityMappings:
        ips:
        - id: ip1
          addressColumn: SrcIpAddr
      tactics:
      - tactic: Persistence
        techniques:
        - technique: T1098
