schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: 51617533-cf51-4415-9020-b15bd47d69d2
    path: Analytics Rules/1Password - Vault export prior to account suspension or deletion.yaml
    version: 1.0.0
    querySha256: 5189fc92a3613357390a52bed0a339555b07aea0a9c0b570156b87111034c437
    schedule:
      queryFrequency: 1h
      queryPeriod: 14d
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    errors: []
    originalTactics:
    - CredentialAccess
    originalTechniques:
    - T1555
properties:
  id: xdr-1password-vault-export-prior-to-account-suspension-or-deletion-51617533
  displayName: 1Password - Vault export prior to account suspension or deletion
  status: disabled
  queryCondition:
    queryText: |-
      let ruleFrequency = 1h;
      let lookback = 14d;
      let onePasswordVaultExportCheck =
          OnePasswordEventLogs_CL
          | where TimeGenerated between (ago(lookback) .. ago(now() - ago(ruleFrequency)))
          | where action == "export" and object_type == "vault"
      ;
      OnePasswordEventLogs_CL
      | where (action == "suspend" and object_type == "user") or (action == "delete" and object_type == "user")
      | extend
          userUuid = tostring(object_details.uuid)
          , auditevents = bag_pack("action", action, "object_type", object_type)
      | join (
          onePasswordVaultExportCheck
          | extend userUuid = tostring(actor_details.uuid)
          )
          on $left.userUuid == $right.userUuid
      | extend
          TargetUsername = actor_details1.email
          , SrcIpAddr = session1.ip_address
      | project
          TimeGenerated
          , auditevents
          , actor_details
          , location_details = location
          , session_details = session
          , exported_vault = object_details
          , TargetUsername
          , SrcIpAddr
  schedule:
    frequency: PT1H
  detectionAction:
    alertTemplate:
      title: 1Password - Vault export prior to account suspension or deletion
      description: |-
        This will alert when a successful vault export has occurred within the last 14 days prior to an account being suspended or deleted from 1Password.

        Ref: https://1password.com/
        Ref: https://github.com/securehats/
      severity: medium
      entityMappings:
        ips:
        - id: ip1
          addressColumn: SrcIpAddr
      tactics:
      - tactic: CredentialAccess
        techniques:
        - technique: T1555
