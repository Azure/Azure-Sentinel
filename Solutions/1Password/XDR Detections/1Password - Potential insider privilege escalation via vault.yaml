schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: a00ffbd8-1d1c-47a3-b0a6-7d70bd8017ed
    path: Analytics Rules/1Password - Potential insider privilege escalation via vault.yaml
    version: 1.0.0
    querySha256: ebe06c3f26ab82266116c16293959b292c2090f17368ee6f27234fc59c4fc3d6
    schedule:
      queryFrequency: 5m
      queryPeriod: 5m
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - source queryPeriod has no direct Custom Detection schedule field; verify the KQL lookback
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    errors: []
    originalTactics:
    - PrivilegeEscalation
    originalTechniques:
    - T1078
properties:
  id: xdr-1password-potential-insider-privilege-escalation-via-vault-a00ffbd8
  displayName: 1Password - Potential insider privilege escalation via vault
  status: disabled
  queryCondition:
    queryText: |-
      OnePasswordEventLogs_CL
      | where log_source == "auditevents"
      | where action has_any("grant", "update")
      | where object_type == "uva"
      | where tostring(actor_details.email) == tostring(aux_details.email)
      | extend
          TargetUsername = aux_details.email
          , ActorUsername = actor_details.email
          , SrcIpAddr = session.ip
  schedule:
    frequency: PT5M
  detectionAction:
    alertTemplate:
      title: 1Password - Potential insider privilege escalation via vault
      description: |-
        This will alert when an actor grants, or updates their own permissions via a vault. Once this analytics rule is triggered it will group all related future alerts for upto an hour when all related entities are the same.

        Ref: https://1password.com/
        Ref: https://github.com/securehats/
      severity: medium
      entityMappings:
        ips:
        - id: ip1
          addressColumn: SrcIpAddr
      tactics:
      - tactic: PrivilegeEscalation
        techniques:
        - technique: T1078
