schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: 76e386eb-f51a-4600-97d1-f0db3b7e41f1
    path: Analytics Rules/1Password - Privileged vault permission change.yaml
    version: 1.0.0
    querySha256: f266950b7387b97f69ef35f27354a20ba9dcc7c4d8d69f7c1d200138a45cf9de
    schedule:
      queryFrequency: 5m
      queryPeriod: 5m
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - source queryPeriod has no direct Custom Detection schedule field; verify the KQL lookback
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    errors: []
    originalTactics:
    - Persistence
    originalTechniques:
    - T1098
properties:
  id: xdr-1password-privileged-vault-permission-change-76e386eb
  displayName: 1Password - Privileged vault permission change
  status: disabled
  queryCondition:
    queryText: |-
      let watchlist =
          _GetWatchlist("PV1PW")
          | project SearchKey
      ;
      // Insert the vault UUIDs below when using the dynamic vaults list within the analytics rule itself
      let vaults = dynamic([""]);
      OnePasswordEventLogs_CL
      | where log_source == "auditevents"
      | where
          (action has_any("grant", "revoke", "update") and object_type == "uva") or
          (action has_any("grant", "revoke", "update") and object_type == "gva")
      // Enable the line below when using the "Privileged Vaults - 1PW" watchlist
      | where object_uuid in (watchlist)
      // Enable the line below when using the dynamic vaults list within the analytics rule itself
      // | where object_uuid in (vaults)
      | extend
          TargetUsername = case(isnotempty(aux_details), aux_details.email, "")
          , TargetGroupUUID = case(isempty(aux_details), aux_uuid, "")
          , ActorUsername = actor_details.email
          , SrcIpAddr = session.ip
  schedule:
    frequency: PT5M
  detectionAction:
    alertTemplate:
      title: 1Password - Privileged vault permission change
      description: |-
        This will alert when permissions have changed within a privileged vault. Once this analytics rule is triggered it will group all related future alerts for upto an hour when all related entities are the same.

        Ref: https://1password.com/
        Ref: https://github.com/securehats/
      severity: high
      entityMappings:
        ips:
        - id: ip1
          addressColumn: SrcIpAddr
      tactics:
      - tactic: Persistence
        techniques:
        - technique: T1098
