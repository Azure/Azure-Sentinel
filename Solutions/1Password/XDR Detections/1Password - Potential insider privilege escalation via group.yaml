schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: 398a1cf1-f56f-4700-912c-9bf4c8409ebc
    path: Analytics Rules/1Password - Potential insider privilege escalation via group.yaml
    version: 1.0.0
    querySha256: 151f3f920bd24bac30c32f731b41727dbab26307d20fe757ff5c4044ba524b7e
    schedule:
      queryFrequency: 5m
      queryPeriod: 5m
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - source queryPeriod has no direct Custom Detection schedule field; verify the KQL lookback
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    - Account mapping is incomplete and was removed
    - Account.FullName has no supported Custom Detection field
    errors: []
    originalTactics:
    - PrivilegeEscalation
    originalTechniques:
    - T1078
properties:
  id: xdr-1password-potential-insider-privilege-escalation-via-group-398a1cf1
  displayName: 1Password - Potential insider privilege escalation via group
  status: disabled
  queryCondition:
    queryText: |-
      OnePasswordEventLogs_CL
      | where log_source == "auditevents"
      | where action has_any("join", "role")
      | where object_type == "gm"
      | where tostring(actor_details.email) == tostring(aux_details.email)
      | extend
          TargetUsername = aux_details.email
          , ActorUsername = actor_details.email
          , SrcIpAddr = session.ip
          , GroupRole = case(
              aux_info == "R", "Group member"
              , aux_info == "A", "Group manager"
              , aux_info
          )
  schedule:
    frequency: PT5M
  detectionAction:
    alertTemplate:
      title: 1Password - Potential insider privilege escalation via group
      description: |-
        This will alert when an actor grants, or updates their own permissions via a group. Once this analytics rule is triggered it will group all related future alerts for upto an hour when all related entities are the same.

        Ref: https://1password.com/
        Ref: https://github.com/securehats/
      severity: medium
      entityMappings:
        ips:
        - id: ip1
          addressColumn: SrcIpAddr
      tactics:
      - tactic: PrivilegeEscalation
        techniques:
        - technique: T1078
