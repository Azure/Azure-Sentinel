| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History** |
|-------------|--------------------------------|--------------------|
| 3.0.0       | 16-03-2026                     | Initial release — Logic App workflow with NDJSON payload parsing (payload.identifier, payload.detection), managed identity authentication for Sentinel createIndicator API, null identifier guard, confidence mapping from Cyren risk score. Content Hub playbook visibility fix (dependency contentId corrected), optional JWT token with conditional deployment, explicit FeedId selection, and per-feed Azure Marketplace trial links. |
