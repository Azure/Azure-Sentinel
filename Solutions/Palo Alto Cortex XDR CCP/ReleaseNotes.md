| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History**                                                       |
|-------------|--------------------------------|--------------------------------------------------------------------------|
| 3.0.5       | 28-05-2026                     | Fixed runaway offset-pagination loop on the `PaloAltoCortexXDR_Alerts_CL` data stream that caused sustained HTTP 429 throttling from the Cortex XDR API. Added explicit `search_from` (initial value `0`) and `search_to` (initial value `100`) keys to the Alerts `queryParametersTemplate` request body, in line with the [Palo Alto Cortex XDR Get All Alerts pagination contract](https://docs-cortex.paloaltonetworks.com/r/Cortex-XDR/Cortex-XDR-Prevent-API-Reference/Get-All-Alerts), and switched the `creation_time` `sort.keyword` from `desc` to `asc` so the CCP RestApiPoller runtime advances pages deterministically and exits the paging loop on a short final page. No changes to the other four data streams (Agent / Endpoints / Incidents / Management). |
| 3.0.4       | 14-10-2025                     | Updating CCF Polling file to implement parameters                           |
| 3.0.3       | 09-04-2025                     | Updating CCF connector parameters                                        |
| 3.0.2       | 10-02-2025                     | Advancing CCF **Data Connector** from Public preview to Global Availability.|
| 3.0.1       | 22-01-2025                     | Added Preview tag to **Data Connector**                                                 |
| 3.0.0       | 17-12-2024                     | Initial Solution Release                                                 |
