schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: 4ab8b09e-3c23-4974-afbe-7e653779eb2b
    path: Analytic Rules/ExcessiveNXDOMAINDNSQueriesStaticThresholdBased.yaml
    version: 1.0.2
    querySha256: 7e86c9740a7e15c8847f98380e2e6d6a88ae856d5b710256b98f7bdba94ec6b8
    schedule:
      queryFrequency: 1h
      queryPeriod: 1h
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - ASIM parser availability must be verified in Advanced Hunting
    errors: []
    originalTactics:
    - CommandAndControl
    originalTechniques:
    - T1568
    - T1008
    acceptedReviewReasons:
    - ASIM parser availability must be verified in Advanced Hunting
properties:
  id: xdr-detect-excessive-nxdomain-dns-queries-static-threshold-based-asim-dns-so-4ab8b09e
  displayName: Detect excessive NXDOMAIN DNS queries - Static threshold based (ASIM DNS Solution)
  status: disabled
  queryCondition:
    queryText: |-
      let lookback = 1h;
      let threshold = 200;
      _Im_Dns(starttime=ago(lookback), responsecodename='NXDOMAIN')
      | summarize NXDOMAINCount=count() by SrcIpAddr, bin(TimeGenerated, 15m)
      | where NXDOMAINCount > threshold
      | join kind=inner (_Im_Dns(starttime=ago(lookback), responsecodename='NXDOMAIN')
        | summarize DNSQueries = makeset(DnsQuery) by SrcIpAddr)
        on SrcIpAddr
      | extend NXDOMAINthreshold=threshold
      | project-away SrcIpAddr1
  schedule:
    frequency: PT1H
  detectionAction:
    alertTemplate:
      title: Detect excessive NXDOMAIN DNS queries - Static threshold based (ASIM DNS Solution)
      description: '''This rule generates an alert when the configured threshold for DNS queries to non-existent domains is
        breached. This helps in identifying possible C2 communications. It utilizes [ASIM](https://aka.ms/AboutASIM) normalization
        and is applied to any source that supports the ASIM DNS schema.'''
      severity: medium
      entityMappings:
        ips:
        - id: ip1
          addressColumn: SrcIpAddr
      tactics:
      - tactic: CommandAndControl
        techniques:
        - technique: T1568
        - technique: T1008
