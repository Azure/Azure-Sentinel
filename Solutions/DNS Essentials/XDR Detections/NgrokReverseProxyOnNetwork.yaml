schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: 50b0dfb7-2c94-4eaf-a332-a5936d78c263
    path: Analytic Rules/NgrokReverseProxyOnNetwork.yaml
    version: 1.0.0
    querySha256: aa5e2488d5cfbe4e0722c47f7de2a4bbe34ffe6aaae13a70aab38923cde31445
    schedule:
      queryFrequency: 1h
      queryPeriod: 1h
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - ASIM parser availability must be verified in Advanced Hunting
    - DNS has no supported Custom Detection mapping
    errors: []
    originalTactics:
    - CommandAndControl
    originalTechniques:
    - T1572
    - T1090
    - T1102
    acceptedReviewReasons:
    - ASIM parser availability must be verified in Advanced Hunting
properties:
  id: xdr-ngrok-reverse-proxy-on-network-asim-dns-solution-50b0dfb7
  displayName: Ngrok Reverse Proxy on Network (ASIM DNS Solution)
  status: disabled
  queryCondition:
    queryText: |-
      // Define a list of Ngrok domains
      let NgrokDomains = dynamic(["ngrok.com", "ngrok.io", "ngrok", "tunnel.com", "korgn", "lennut.com"]);
      // Query the _Im_Dns function for the past 1 hour
      _Im_Dns(starttime=ago(1h))
      | where isnotempty(DnsQuery) // Filter out empty DNS queries
      | where DnsQuery has_any (NgrokDomains) // Filter DNS queries that match any of the Ngrok domains
      | summarize Starttime = min(EventStartTime),Endtime=max(EventEndTime),EventsCount=sum(EventCount),EventResults=make_set(EventResult,4) by DnsQuery, Domain, SrcIpAddr, Dvc
      // Summarize the data by Domain, DNS query, source IP address, and device Dvc
  schedule:
    frequency: PT1H
  detectionAction:
    alertTemplate:
      title: Ngrok Reverse Proxy on Network (ASIM DNS Solution)
      description: '''This detection identifies the top four Ngrok domains from DNS resolution. Ngrok reverse proxy can bypass
        network defense. While not inherently harmful, it has been used for malicious activities recently.'''
      severity: medium
      entityMappings:
        ips:
        - id: ip1
          addressColumn: SrcIpAddr
      tactics:
      - tactic: CommandAndControl
        techniques:
        - technique: T1572
        - technique: T1090
        - technique: T1102
