schemaVersion: 1.0.0
kind: CustomDetection
resourceType: Microsoft.Security/detectionRules
apiVersion: 2026-06-01-preview
contentProvenance:
  source:
    platform: Microsoft Sentinel
    kind: AnalyticsRule
    id: 0fe6bde4-b215-480c-99b4-84a96edcdbd7
    path: Analytic Rules/RareClientObservedWithHighReverseDNSLookupCountAnomalyBased.yaml
    version: 1.0.2
    querySha256: ec6fff3093ea49471233012c318abb3ee20aee26e15bfed4df620c0ad7a4dbed
    schedule:
      queryFrequency: 1d
      queryPeriod: 14d
  conversion:
    tool: sentinel-to-xdr-migration
    version: 0.1.0
    status: converted
    reviewRequired: false
    reviewReasons: []
    requiredWorkloads:
    - sentinel
    warnings:
    - ASIM parser availability must be verified in Advanced Hunting
    - IP.Address column `SrcIpAddr` is not produced by the query
    - entity mappings were resolved by the configured per-rule override
    errors: []
    originalTactics:
    - Reconnaissance
    originalTechniques:
    - T1590
    acceptedReviewReasons:
    - ASIM parser availability must be verified in Advanced Hunting
properties:
  id: xdr-rare-client-observed-with-high-reverse-dns-lookup-count-anomaly-based-as-0fe6bde4
  displayName: Rare client observed with high reverse DNS lookup count - Anomaly based (ASIM DNS Solution)
  status: disabled
  queryCondition:
    queryText: |-
      let threshold = 2.5;
      let SearchDomain = dynamic(["in-addr.arpa"]);
      let min_t = ago(14d);
      let max_t = now();
      let timeframe = 1d;
      let DNSEvents=(stime: datetime, etime: datetime) {
        _Im_Dns(starttime=stime, endtime=etime, domain_has_any=SearchDomain)
      };
      DNSEvents(stime=min_t, etime=max_t)
      | make-series QueryCount=dcount(DnsQuery) on TimeGenerated from min_t to max_t step timeframe by SrcIpAddr
      | extend (anomalies, score, baseline) = series_decompose_anomalies(QueryCount, threshold, -1, 'linefit')
      | mv-expand anomalies, score, baseline, TimeGenerated, QueryCount
      | extend
        anomalies = toint(anomalies),
        score = toint(score),
        baseline = toint(baseline),
        EventTime = todatetime(TimeGenerated),
        Total = tolong(QueryCount)
      | where EventTime >= ago(timeframe)
      | where score >= threshold * 2
      | join kind = inner (DNSEvents(stime=ago(timeframe), etime=max_t)
        | summarize DNSQueries=make_set(DnsQuery, 1000) by SrcIpAddr)
        on SrcIpAddr
      | project-away SrcIpAddr1
  schedule:
    frequency: P1D
  detectionAction:
    alertTemplate:
      title: Rare client observed with high reverse DNS lookup count - Anomaly based (ASIM DNS Solution)
      description: '''This rule makes use of the series decompose anomaly method to identify clients with high reverse DNS
        counts. This helps in detecting the possible initial phases of an attack, like discovery and reconnaissance. It utilizes
        [ASIM](https://aka.ms/AboutASIM) normalization and is applied to any source that supports the ASIM DNS schema.'''
      severity: medium
      entityMappings:
        ips:
        - id: ip1
          addressColumn: SrcIpAddr
      tactics:
      - tactic: Reconnaissance
        techniques:
        - technique: T1590
