| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History**                           |
|-------------|--------------------------------|----------------------------------------------|
| 4.2.0       | 11-08-2026                     | Added a **Watchlist** (`Prancer Crown Jewel Assets`) so customers can declare asset criticality independent of SwarmHack engagement history, plus an **Analytic Rule** that escalates any finding matching a watchlist-declared crown jewel; added an optional companion **Azure Function** (`Data Connectors/PrancerConnectivityHealthCheck/`) that independently verifies `PrancerFindings_CL` ingestion is still flowing and reports via webhook |
| 4.1.0       | 11-08-2026                     | Phase 2 content enrichment: consolidated the per-resource-type CSPM **Analytic Rules** into finding-class rules with a confidence-weighted suppression rule; added correlation, confidence, and kill-chain-aware **Analytic Rules** and **Hunting Queries**; added kill-chain, crown-jewel, and MITRE-coverage **Workbook** visualizations; added a trust-tiered **Playbooks** set (autonomous enrichment/ticketing/notification playbooks plus one human-approval-gated notification playbook) |
| 4.0.0       | 10-08-2026                     | Modernized the solution for Prancer's Logs Ingestion API architecture: added a **Parser** (`PrancerFindings`) over the new `PrancerFindings_CL` table/schema, rewrote the **Data Connector** to describe DCR-based push ingestion instead of the retired HTTP Data Collector API, and rewrote all **Analytic Rules**, **Hunting Queries**, and the **Workbook** to query through the parser instead of re-deriving fields from the legacy flattened schema |
| 3.0.1       | 19-03-2024                     | Updated **Workbook**, **Analytic Rules** and **Hunting Queries**                    |
| 3.0.0       | 20-09-2023                     | Initial Solution Release                    |