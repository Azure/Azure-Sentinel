{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "keyVaultName": {
            "type": "String",
            "metadata": {
                "description": "The name of the key vault."
            }
        },
        "apiKey": {
            "type": "securestring",
            "metadata": {
                "description": "The API key value to store in the secret."
            }
        }
    },
    "variables": {
        "secretName": "RecordedFuture",
        "secretValueP1": "{'AuthenticationType': 'Static', 'Properties': {'apiKey': '<apiKey>'}}",
        "secretValue": "[replace(variables('secretValueP1'),'<apiKey>',parameters('apiKey'))]"
    },
    "resources": [
        {
            "type": "Microsoft.KeyVault/vaults/secrets",
            "apiVersion": "2021-06-01-preview",
            "name": "[concat(parameters('keyVaultName'), '/', variables('secretName'))]",
            "location": "[resourceGroup().location]",
            "properties": {
                "value": "[variables('secretValue')]"
            }
        }
    ],
    "outputs": {}
}
