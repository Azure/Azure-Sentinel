| **Version** | **Date Modified (DD-MM-YYYY)** | **Change History**                                                                 |
|-------------|--------------------------------|------------------------------------------------------------------------------------|
| 1.0.1       | 14-09-2026                     | Schema corrected to match the live Frends Platform API response: events path `$.data[*]`; source fields `action`, `user`, `timestampUtc`, `description`, `parameters`; added `Description` column; `parameters` parsed from JSON string via `todynamic()`. Corrected the Scuba service-tag CLI example to return the full prefix list. |
| 1.0.0       | 07-07-2026                     | Initial release. Frends iPaaS Audit Logs data connector (Codeless Connector Framework) ingesting the Frends Platform API Audit Trail Log into `FrendsAuditLogs_CL`. |
